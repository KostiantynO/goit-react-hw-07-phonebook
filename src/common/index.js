export { default as React, StrictMode } from 'react';
export { default as ReactDOM, render } from 'react-dom';
export { default as styled, ThemeProvider } from 'styled-components';
export { default as PropTypes } from 'prop-types';
export { theme } from './theme';

export const LS_KEY = 'phonebook-contacts';

// common components
export { Section, Container, Button, Label, Input } from 'common/components';

// import { StrictMode, render, nanoid, styled, ThemeProvider, PropTypes, theme, Container, Section } from 'common'
